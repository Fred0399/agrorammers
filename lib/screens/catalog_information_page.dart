import 'package:flutter/material.dart';

class CatalogInformationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.green,
      body: ListView(
          //children: dummy.map((title) => CatalogItem(title)).toList(),
          ),
    );
  }
}
